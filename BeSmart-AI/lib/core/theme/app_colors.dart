import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const accent = Color(0xFF007AFF);
  static const accentDark = Color(0xFF0A84FF);

  static const userBubbleLight = Color(0xFF007AFF);
  static const assistantBubbleLight = Color(0xFFE9E9EB);
  static const userBubbleDark = Color(0xFF0A84FF);
  static const assistantBubbleDark = Color(0xFF2C2C2E);

  static const backgroundLight = Color(0xFFF2F2F7);
  static const backgroundDark = Color(0xFF000000);
  static const surfaceLight = Color(0xFFFFFFFF);
  static const surfaceDark = Color(0xFF1C1C1E);

  static const textPrimaryLight = Color(0xFF000000);
  static const textSecondaryLight = Color(0xFF8E8E93);
  static const textPrimaryDark = Color(0xFFFFFFFF);
  static const textSecondaryDark = Color(0xFF8E8E93);

  static const error = Color(0xFFFF3B30);
  static const success = Color(0xFF34C759);
}
